I have screenshots of a UI template as design references provided in stitch_route_management folder.

IMPORTANT:
- Do NOT recreate the entire admin template.
- Do NOT modify the existing application layout.
- Do NOT generate a new sidebar, topbar, footer, navigation, routing structure, or dashboard shell.
- Assume the project already has its own layout implemented.
- Only extract and implement the page content that belongs inside the main content area.

Your task:
1. Analyze the provided screenshot.
2. Identify all content sections, cards, forms, tables, charts, widgets, filters, buttons, badges, and other UI elements inside the page body.
3. Recreate those elements using the project's existing component architecture.
4. Preserve the visual hierarchy and user experience.
5. Adapt the design to fit naturally within the existing application layout.
6. Reuse existing components whenever possible.
7. Follow the project's coding standards and styling conventions.
8. Make the implementation responsive.

When generating code:
- Provide complete component code.
- Include necessary TypeScript interfaces/types.
- Include state management logic if needed.
- Include sample data where backend integration is not yet available.
- Clearly indicate where API integration should occur.

Ignore:
- Sidebar design
- Header design
- Footer design
- Breadcrumb styling from the template
- Theme-specific layout wrappers
- Template-specific navigation elements

Only implement what would appear inside:

<main>
    <!-- page content from screenshot goes here -->
</main>

Treat the screenshot as a visual reference, not as a template that must be copied pixel-for-pixel.